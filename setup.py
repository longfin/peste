# -*- coding: utf-8 -*-
try:
    from setuptools import setup
except ImportError:
    from distutils.core import setup

setup(name="peste",
      description="Simple task distribution system",
      version="0.0.1",
      setup_requires=[],
      install_requires=[],
      extras_require = {},
      entry_points={})
